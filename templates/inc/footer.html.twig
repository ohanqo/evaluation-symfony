<footer class="page-footer font-small bg-light py-4">
    <div class="container-fluid text-center text-md-left">
        <div class="row">
            <div class="col-md-6 text-center text-uppercase my-auto">
                <h5>{{ site_name }}</h5>
            </div>
            <div class="col-md-3 mb-md-0 mb-3">
                <h5 class="text-uppercase">&nbsp;</h5>
                <ul class="list-unstyled">
                    <li>
                        <a href="{{ url('home') }}">Accueil</a>
                    </li>
                    <li>
                        <a href="{{ url('exhibition.index') }}">Expositions</a>
                    </li>
                    <li>
                        <a href="{{ url('contact.index') }}">Contact</a>
                    </li>
                    <li>
                        <a href="{{ url('home.legal') }}">Mentions légales</a>
                    </li>
                </ul>

            </div>
            <div class="col-md-3 mb-md-0 mb-3">
                <h5>Œuvres</h5>
                <ul class="list-unstyled">
                    <li>
                        <a href="{{ url('artwork.index') }}">Tout</a>
                    </li>

                    {% for category in categories %}
                        <li>
                            <a href="{{ url('category.show', {'id': category.id}) }}">{{ category.name }}</a>
                        </li>
                    {% endfor %}
                </ul>
            </div>
        </div>
    </div>
</footer>